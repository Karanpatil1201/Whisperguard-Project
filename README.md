# WhisperGuard

Local-first microphone monitoring prototype (scaffold).

Run the basic CLI after installing requirements.

Requirements:
- Python 3.8+

Quick start (Windows Powershell):

```powershell
pip install -r requirements.txt
python -m whisperguard.main --help
```

This repository contains scaffolding for the WhisperGuard system described in the design document.
